import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "Customers"
};

const Page = () => {
  return (
    <div>
      <p>Customer Page</p>
    </div>
  );
};

export default Page;
